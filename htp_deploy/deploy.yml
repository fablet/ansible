---

- name: Deploy {{ project_name }} server
  remote_user: {{ ansible_user }}
  gather_facts: false
  become: yes

  vars_files:
    - env_vars/protected.yml
    - env_vars/base.yml

  roles:
    - deploy
